import path from 'path';import fs from 'fs';import { app } from 'electron';import type { AppConfig,MediaInfo,OutputConfig,Encoder } from '../shared/types';
export function ffmpegPath(){return app.isPackaged?path.join(process.resourcesPath,'ffmpeg','ffmpeg.exe'):path.join(process.cwd(),'resources','ffmpeg',process.platform==='win32'?'ffmpeg.exe':'ffmpeg')}
export function ffprobePath(){return app.isPackaged?path.join(process.resourcesPath,'ffmpeg','ffprobe.exe'):path.join(process.cwd(),'resources','ffmpeg',process.platform==='win32'?'ffprobe.exe':'ffprobe')}
export function maskSecret(s:string){if(!s)return '';return s.length<9?'••••••••':`${s.slice(0,4)}••••••••${s.slice(-4)}`}
export function sanitizeArgs(args:string[]){return args.map(a=>a.replace(/(rtmps?:\/\/[^\s/]+\/[^\s/]+\/)[^\s]+/g,'$1<redacted>'))}
export function buildFilter(o:OutputConfig):string{const W=o.width,H=o.height;if(o.fit==='stretch')return `scale=${W}:${H},setsar=1`;if(o.fit==='fit')return `scale=${W}:${H}:force_original_aspect_ratio=decrease,pad=${W}:${H}:(ow-iw)/2:(oh-ih)/2:black,setsar=1`;if(o.fit==='original')return `pad=${W}:${H}:(ow-iw)/2:(oh-ih)/2:black,crop=${W}:${H},setsar=1`;const z=Math.max(1,o.zoom/100),x=`(iw-${W})*${Math.max(0,Math.min(100,o.cropX))/100}`,y=`(ih-${H})*${Math.max(0,Math.min(100,o.cropY))/100}`;return `scale=iw*${z}:ih*${z},scale=${W}:${H}:force_original_aspect_ratio=increase,crop=${W}:${H}:${x}:${y},setsar=1`}
export function encoderArgs(enc:Encoder,b:number,fps:number){const g=fps*2;if(enc==='nvenc')return ['-c:v','h264_nvenc','-preset','p5','-rc','cbr','-b:v',`${b}k`,'-maxrate',`${b}k`,'-bufsize',`${b*2}k`,'-g',String(g)];if(enc==='qsv')return ['-c:v','h264_qsv','-b:v',`${b}k`,'-maxrate',`${b}k`,'-bufsize',`${b*2}k`,'-g',String(g)];if(enc==='amf')return ['-c:v','h264_amf','-rc','cbr','-b:v',`${b}k`,'-maxrate',`${b}k`,'-bufsize',`${b*2}k`,'-g',String(g)];return ['-c:v','libx264','-preset','veryfast','-tune','zerolatency','-b:v',`${b}k`,'-maxrate',`${b}k`,'-bufsize',`${b*2}k`,'-g',String(g),'-keyint_min',String(g)]}
export function buildStreamArgs(media:MediaInfo,o:OutputConfig,cfg:AppConfig,encoder:Encoder){const dest=`${o.server.replace(/\/$/,'')}/${o.key}`;const a=['-hide_banner','-nostdin','-re'];if(cfg.loop)a.push('-stream_loop','-1');a.push('-i',media.path);const silent=!(cfg.audioEnabled&&media.hasAudio);if(silent)a.push('-f','lavfi','-i','anullsrc=channel_layout=stereo:sample_rate=44100');const add=(target:string,format:string)=>{a.push('-vf',buildFilter(o),'-r',String(o.fps),'-pix_fmt','yuv420p',...encoderArgs(encoder,o.bitrateKbps,o.fps));if(!silent)a.push('-c:a','aac','-b:a','160k','-ac','2','-af',`volume=${cfg.audioVolume/100}`);else a.push('-c:a','aac','-b:a','128k','-ac','2');a.push('-f',format,target)};add(dest,'flv');if(cfg.recordLocal&&cfg.recordDir)add(path.join(cfg.recordDir,`${o.width>o.height?'horizontal':'vertical'}-${Date.now()}.mkv`),'matroska');return a}
export function ffmpegExists(){return fs.existsSync(ffmpegPath())&&fs.existsSync(ffprobePath())}