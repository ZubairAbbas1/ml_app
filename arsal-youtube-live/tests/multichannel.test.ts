import{describe,it,expect}from'vitest';import{defaultConfig}from'../src/shared/defaults';import{destinationsForOutput,buildStreamArgs}from'../src/main/ffmpeg';
describe('multi-channel streaming',()=>{it('includes primary plus enabled same-format channels',()=>{const cfg=structuredClone(defaultConfig);cfg.horizontal.key='primary-key';cfg.multiDestinations=[{id:'a',name:'Second',kind:'horizontal',enabled:true,server:'rtmps://a.rtmps.youtube.com/live2',key:'second-key'},{id:'b',name:'Vertical extra',kind:'vertical',enabled:true,server:'rtmps://a.rtmps.youtube.com/live2',key:'vertical-key'}];expect(destinationsForOutput(cfg.horizontal,cfg)).toHaveLength(2)});it('uses tee muxer when multiple targets are active',()=>{const cfg=structuredClone(defaultConfig);cfg.horizontal.key='primary-key';cfg.multiDestinations=[{id:'a',name:'Second',kind:'horizontal',enabled:true,server:'rtmps://a.rtmps.youtube.com/live2',key:'second-key'}];const media=[{path:'C:/video.mp4',name:'video.mp4',size:1,duration:30,width:1920,height:1080,fps:30,videoCodec:'h264',hasAudio:true,audioCodec:'aac',audioChannels:2}];const args=buildStreamArgs(media,undefined,cfg.horizontal,cfg,'cpu');expect(args).toContain('tee');expect(args.join(' ')).toContain('onfail=ignore')});});
