import{describe,it,expect}from'vitest';import{defaultConfig}from'../src/shared/defaults';import{destinationsForOutput,buildEncoderRelayArgs,buildRelayArgs}from'../src/main/ffmpeg';
describe('multi-channel streaming',()=>{
 it('includes primary plus enabled same-format channels',()=>{const cfg=structuredClone(defaultConfig);cfg.horizontal.key='primary-key';cfg.multiDestinations=[{id:'a',name:'Second',kind:'horizontal',enabled:true,server:'rtmps://a.rtmps.youtube.com/live2',key:'second-key',priority:'normal',group:'Main'},{id:'b',name:'Vertical extra',kind:'vertical',enabled:true,server:'rtmps://a.rtmps.youtube.com/live2',key:'vertical-key',priority:'backup',group:'Vertical'}];const d=destinationsForOutput(cfg.horizontal,cfg);expect(d).toHaveLength(2);expect(d[0].priority).toBe('primary')});
 it('encodes once to isolated local relay targets',()=>{const cfg=structuredClone(defaultConfig),media=[{path:'C:/video.mp4',name:'video.mp4',size:1,duration:30,width:1920,height:1080,fps:30,videoCodec:'h264',hasAudio:true,audioCodec:'aac',audioChannels:2}],args=buildEncoderRelayArgs(media,undefined,cfg.horizontal,cfg,'cpu',[{id:'one',port:24100},{id:'two',port:24101}]);expect(args).toContain('tee');expect(args.join(' ')).toContain('udp://127.0.0.1:24100');expect(args.join(' ')).toContain('udp://127.0.0.1:24101')});
 it('relay remuxes without a second encode',()=>{const a=buildRelayArgs(24100,'rtmps://host/live/key');expect(a.join(' ')).toContain('-c copy');expect(a.join(' ')).toContain('-f flv')});
});
